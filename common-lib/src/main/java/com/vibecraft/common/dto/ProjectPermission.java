package com.vibecraft.common.dto;

/** Wire copy of workspace-service's {@code ProjectPermission} vocabulary — see {@link ProjectRole}. */
public enum ProjectPermission {
    VIEW,
    EDIT,
    DELETE,
    MANAGE_MEMBERS,
    VIEW_MEMBERS
}
