# VibeCraft — Project Documentation

Living documentation of the project, kept current with each commit. Reference for APIs, entities, tech stack, and practices used — useful for resume/portfolio write-ups later.

## Contents

- [Overview](overview.md) - What VibeCraft is and who it is for
- [Tech Stack](tech-stack.md) - Languages, frameworks and libraries, and why each is here
- [Architecture](architecture/README.md) - How the system is put together, request by request
- [API](api/README.md) - Every endpoint, its request and response shapes, and errors
- [Schema](schema/README.md) - Entities, tables, enums and schema conventions
- [Practices](practices/README.md) - Conventions, gotchas, security guardrails, testing, definition of done
- [Project Status](project-status.md) - What is built and what is next

## Related docs

- [README.md](../README.md) — public-facing project overview and setup instructions.
- [CLAUDE.md](../CLAUDE.md) — instructions for AI coding assistants working in this repo.

---

_Last updated: 2026-05-24 (v3 entity schema: simplified ownership/roles/chat/usage designs, restored JPA annotations after an external revert; added repository, MapStruct mapper, and global exception handling layers; first real service logic in `ProjectServiceImpl`; `application.yaml`/`data.sql` are now committed; `ProjectServiceImpl` completed — `getUserProjectById`, `updateProject`, `softDelete` implemented alongside a new `findAccessibleProjectById` repository query and a `ForbiddenException`/403 handler; `ProjectMemberServiceImpl` completed — all 4 methods implemented alongside new `ProjectMemberRepository`/`ProjectMemberMapper`/`UserRepository.findByEmail`, `ProjectRole.OWNER` re-added, `data.sql` now seeds 3 users; `GlobalExceptionHandler` gained a `MethodArgumentNotValidException` → 400 handler, closing the last "Next up" item from the previous pass; `LoginRequest.password` gained an undocumented, message-less `@Size(min = 8)`; **v4 schema**: `Project.owner` FK removed in favor of `ProjectMember.projectRole == OWNER`, dropping every owner-only authorization check in the process (flagged as the top "Known gap"); `User.email`/`passwordHash` renamed back to `username`/`password`, `avatarUrl` dropped, propagated throughout; `data.sql` seeds `password = 'N/A'`; local `ddl-auto` switched to `create`; **JWT auth landed**: `spring-boot-starter-security` + JJWT added, new `security` package (`WebSecurityConfig`/`JwtAuthFilter`/`AuthUtil`/`JwtUserPrincipal`), `User implements UserDetails`, `AuthServiceImpl.signup`/`login` fully implemented (BCrypt hashing, real JWTs — though `signup`'s response bug returns the literal string `"dummy"` instead of the generated token), `UserServiceImpl` implements `UserDetailsService`; every controller/service dropped its explicit `userId` parameter in favor of `AuthUtil.getCurrentUserId()`; `ProjectRepository`'s two accessibility queries rewritten as `EXISTS` subqueries (fixing an entity-name typo introduced in that rewrite); `data.sql` deleted (real signup replaces the seed), `ddl-auto` reverted to `update`; the JWT secret briefly had no env-var fallback at all — caught and fixed (`${JWT_SECRET:...}`) before reaching `main`; **full endpoint test pass + authorization/error-handling hardening**: added `@PreAuthorize`-based role authorization (`ProjectPermission` enum, `ProjectRole` permission sets, `SecurityExpressions`, `ProjectMemberRepository.findRoleByProjectIdAndUserId`) closing the long-flagged "no owner-only authorization" gap; fixed `AuthServiceImpl.signup` actually returning its generated token instead of `"dummy"`; replaced bare `Optional.orElseThrow()`/`RuntimeException` in `ProjectMemberServiceImpl` with typed `ResourceNotFoundException`; fixed `UserServiceImpl.loadUserByUsername` to throw the contractually-correct `UsernameNotFoundException`; expanded `GlobalExceptionHandler` from 4 to 11 handlers (auth failures, JWT errors, malformed requests, type mismatches, DB constraint violations, catch-all) so no endpoint falls through to Spring's default error format; manually verified all 9 real `Project`/`ProjectMember` endpoints' happy and error paths end-to-end; **accept-invite endpoint added**: new `POST /api/projects/{projectId}/members/accept` (self-scoped, idempotent, 404 if never invited) finally exercises `ProjectMember.acceptedAt`, which had existed unused on the entity since v1; `MemberResponse` gained an `acceptedAt` field; removed the dead, unused `ProjectMemberMapper.toProjectMemberResponseFromOwner` method found while making this change; **2026-05-02, real Stripe billing**: added the `stripe-java` 31.1.0 SDK and a `config.PaymentConfig` that sets `Stripe.apiKey` from a new `stripe.api.secret` property at startup; `User` gained a unique `stripeCustomerId` column, moved off `Subscription` where it had been duplicated; added `PlanRepository`/`SubscriptionRepository` (5 of 10 entities now have a repository) and `SubscriptionMapper` (4 of 10 now have a mapper); `SubscriptionService`/`SubscriptionServiceImpl` gained real `getCurrentSubscription()`, `activateSubscription`/`updateSubscription`/`cancelSubscription`/`renewSubscriptionPeriod`/`markSubscriptionPastDue` (idempotent activation via `existsByStripeSubscriptionId`) — every `SubscriptionService` method landed real in this same commit, though this doc incorrectly listed `getCurrentSubscription()` as a stub until it was caught and corrected 2026-05-04; new `PaymentProcessor` interface + `StripePaymentProcessor` implementation with a real `createCheckoutSessionUrl` (subscription-mode Stripe Checkout, reuses `User.stripeCustomerId` when present) and a real `handleWebhookEvent` dispatching Stripe's `checkout.session.completed`/`customer.subscription.updated`/`.deleted`/`invoice.paid`/`invoice.payment_failed` events to the matching `SubscriptionService` method; `BillingController`'s checkout/portal endpoints now delegate to `PaymentProcessor` instead of stubbing, and gained a 7th, real endpoint, `POST /webhooks/payment` (signature-verified via `Webhook.constructEvent`, permitted by the existing `/webhooks/**` rule). Found and fixed along the way: a `PaymentConfig`/`application.yaml` property-path mismatch (`stripe.api.secret` vs `stripe.secret`) that failed startup outright; a `@Value` field silently dropped from DI because `@FieldDefaults(makeFinal = true)` swept it into a Lombok-generated constructor with no `@Value` annotation carried over (fixed with `@NonFinal` on that one field); the two new Stripe secrets were briefly committed as real, unwrapped plaintext values in `application.yaml` before being caught and replaced with placeholder-default env-var placeholders, unlike the JWT/DB pattern, since — unlike those — they're tied to a real external account and this repo is public; **2026-05-04**: added `.claude/launch.json` so the app can be run and its logs inspected directly from a Claude Code session; added `spring.config.import: optional:file:.env[.properties]` to `application.yaml` and gitignored `.env`, so a local, never-committed `.env` file can supply real values for the env-var placeholders (`DB_USERNAME`, `DB_PASSWORD`, `JWT_SECRET`, `STRIPE_SECRET`, `STRIPE_WEBHOOK_SECRET`) without editing tracked config — verified with a temporary log statement (removed before committing) that the real value actually flows through from `.env` rather than each property's inline default; `StripePaymentProcessor.openCustomerPortal()` implemented for real (creates a Stripe billing-portal session, 400 if the caller has never checked out) — every billing service method is now real, `FileServiceImpl`/`PlanServiceImpl`/`UsageServiceImpl` are the only ones still stubbed; **plan-based project limits**: `SubscriptionService` gained `canCreateNewProject()` (compares `ProjectMemberRepository.countProjectOwnedByUser` against the caller's plan's `maxProjects`, or a hardcoded 100 with no active subscription — flagged as a "Known gap" for being inconsistently sourced from code vs. the database), and `ProjectServiceImpl.createProject` now calls it first, 400 `BadRequestException` if the caller is at their limit; fixed before committing: `countProjectOwnedByUser`'s initial JPQL counted soft-deleted projects toward the limit, contradicting this codebase's established "exclude soft-deleted rows manually" convention — added the missing `pm.project.deletedAt IS NULL` filter; **2026-05-16, v5 schema + real AI chat generation**: `ChatEvent`/`ChatEventType` restored from the original v2 design (`ChatEventType` was sitting on disk with its old reference-project package the whole time) in place of `ChatMessage.toolCalls`; `ChatSession.title` dropped again; `UsageLog` reverted to a v2-style per-user daily counter. Added the `spring-ai-starter-model-openai` (OpenRouter-compatible) and `io.minio:minio` dependencies — the latter transitively pulled a near-empty, Kotlin-Multiplatform-only `okhttp:5.1.0` that plain Maven can't resolve past its thin compatibility jar, fixed by pinning `okhttp:4.12.0` directly. New `config.AiConfig`/`StorageConfig` (`ChatClient`/`MinioClient` beans), a `minio` config block, and `spring.ai.openai.api-key` sourced from a new `OPENROUTER_API_KEY` env var (found hardcoded in plaintext before committing, same treatment as the Stripe secrets). New `llm` package: `PromptUtils` (the code-gen system prompt — fixed a bug where `LocalDateTime.now()` was baked into a `static final` constant, freezing the "current time" the model sees at server-startup time forever; converted to a method), `LlmResponseParser` (parses `<message>`/`<file>`/`<tool>` tags out of streamed output into `ChatEvent`s), `llm.tools.CodeGenerationTools` (the `read_files` tool), `llm.advisors.FileTreeContextAdvisor` (injects the file tree as context). New, real `ProjectFileService`/`ProjectFileServiceImpl` (MinIO-backed) — but this is a separate abstraction from the pre-existing, still-stub `FileService` that `FileController`'s public endpoints actually call (fixed a same-batch bug where it read from a hardcoded bucket name but wrote to the configured one). New `ChatService`/`ChatServiceImpl` (history) and `AiGenerationService`/`AiGenerationServiceImpl` (the streaming generation pipeline — fixed a bug where a missing `usage` object from the model provider would NPE and silently drop the whole chat exchange), wired up via a new `ChatController` (`POST /api/chat/stream`, `GET /api/chat/projects/{projectId}`). Entity count now 11, repositories 10 of 11 (only `Preview` lacks one), mappers 6 of 11, controllers 7, service interfaces 11. New known gaps: `ChatRequest` has no Bean Validation at all; `GET /api/chat/projects/{projectId}` has no `@PreAuthorize` check; the `ASSISTANT` `ChatMessage.content` is still a hardcoded placeholder, not the model's real output; `FileController`/`ProjectFileService` remain unmerged; `canCreateNewProject()`'s free-tier constant still isn't DB-driven; **2026-05-16, later pass — dead-code cleanup and local environment realignment**: deleted `FileService`/`FileServiceImpl`, repointing `FileController.getFileTree` at `ProjectFileService` directly, closing the file-storage-duplication known gap; added `services.docker-compose.yml` and updated `application.yaml`'s datasource/MinIO config to match this project's own container names/ports instead of the reference project it was adapted from; switched the configured OpenRouter model to `x-ai/grok-4.5` after live-verifying several free-tier alternatives each failed differently (empty output, a 404 on a stale-training-data model slug, a malformed tool-call name, and persistent free-tier rate-limiting later confirmed to be genuine provider-side capacity contention rather than an account-level cap); **2026-05-16, later pass — chat request validation and history authorization**: `ChatRequest` gained `@NotBlank message`/`@NotNull projectId` and `@Valid` on the controller, closing known-gap #9's validation half; `GET /api/chat/projects/{projectId}` gained `@PreAuthorize("@security.canViewProject(#projectId)")`, closing the other half; `ChatServiceImpl.getProjectChatHistory` switched from `chatSessionRepository.getReferenceById` (which threw lazily on first access to an unresolved proxy) to `findById`, returning an empty list for a project with no chat session yet instead of erroring; `ChatController`'s SSE error handler now distinguishes an OpenRouter 429 (walking the exception's cause chain) and returns a specific rate-limited message instead of a generic one. Verified live via curl against a running instance: blank/null `ChatRequest` fields -> 400 with field errors; history for an unmessaged project -> 200 + `[]`; history/streaming for a project the caller doesn't own -> 403); **2026-05-16, later pass — AI generation resilience and diagnostic logging**: re-enabled `AiGenerationServiceImpl`'s OpenRouter-429 retry (`Flux.defer` + `Retry.backoff`, 3 attempts) after temporarily disabling it mid-session to conserve a scarce free-tier daily quota; `finalizeChats`'s file-save loop now isolates each file's save in its own try/catch instead of one `forEach` that aborted the whole batch (and the chat event history) on the first failure; `LlmResponseParser` now logs a WARN with a content preview for any text outside its recognized tags instead of silently dropping it, and skips a `<file>` tag with no `path` attribute instead of producing a `ChatEvent` that would later NPE inside `ProjectFileServiceImpl.saveFile`; `CodeGenerationTools.readFiles` guards a null/empty `paths` argument. Found and fixed along the way: the first `Flux.defer`-less retry implementation threw `IllegalStateException: No StreamAdvisors available to execute` on its second attempt, since Spring AI's advisor chain is single-use per subscription — see the new Practices bullet); **2026-05-16, later pass — typed storage exceptions, idempotent template init with retry, non-blocking project creation**: new `FileStorageException` (→ 503) replaces bare `RuntimeException` in `ProjectFileServiceImpl`, which now also distinguishes MinIO's `NoSuchKey` (→ `ResourceNotFoundException`, 404) from any other storage error, and `saveFile` guards a null/blank path with `BadRequestException` instead of NPEing on `path.startsWith("/")`. `ProjectTemplateService`/`ProjectTemplateServiceImpl` — never previously committed as its own concern despite existing as uncommitted work — is now idempotent (skips files a project already has, safe to call more than once) and auto-retries up to 3 times, returning a `TemplateInitResult` instead of throwing on partial failure; `ProjectServiceImpl.createProject` no longer lets a template-init failure block or roll back project creation (considered and rejected auto-retry-then-fail-creation-entirely as an alternative — it couples project creation to MinIO's availability and risks a retry storm worsening a real outage under concurrent creates), instead recording what's missing on a new nullable `Project.templateInitIssue` column, surfaced on `ProjectResponse` and clearable via new `POST /api/projects/{id}/retry-template-init`; `FileTreeContextAdvisor` now tells the AI directly when a project's template didn't fully initialize, appending a NOTICE block after the FILE_TREE. `LoginRequest.password`'s `@Size` gained an explicit `message`, closing the last remaining message-less constraint in the codebase. Verified live end-to-end by deliberately stopping the MinIO container mid-request: project creation still succeeded (non-blocking confirmed), the broken project had zero files, the retry endpoint fixed it completely once MinIO came back, and `templateInitIssue` cleared correctly. One real bug found this way and fixed before committing: the incomplete-template message read "1 file(s) could not be created ((unknown file))" when MinIO was fully unreachable — misleading, since literally zero of 15 files were attempted; now says the storage was unreachable when nothing succeeded at all, only itemizing specific paths when some files did copy; **2026-05-24 — abandoned-edit retry safety net**: found live while investigating a user report that AI-requested file edits weren't showing up in MinIO — turned out two consecutive chat turns each completed in 1–2 seconds with ~105 tokens, stopping right after the model's `<tool>` narration with no `<file>` tag ever emitted, so there was genuinely nothing to save (not a storage bug, confirmed by reproducing a normal edit against the same pipeline immediately after, which worked end-to-end). `AiGenerationServiceImpl.finalizeChats` now detects this exact pattern (`TOOL_LOG` present, zero `FILE_EDIT`) and automatically retries the whole generation once, non-streamed; if the retry is also empty, a plain-language "wasn't able to finish this" message is appended instead of silently persisting an empty-looking success. Refactored the streaming call's `Flux.defer`/429-retry wrapper into a shared `buildGenerationFlux` so both the live SSE path and the new internal retry path use identical resilience logic; **2026-05-24 — file metadata population and a missing-parameter 500**: `FileNode.modifiedAt`/`size`/`type` had been returning `null` on every `GET /api/projects/{id}/files` response since the endpoint existed — `ProjectFile` never had `size`/`type` columns, and `modifiedAt` didn't name-match the entity's `updatedAt`, so MapStruct's auto-mapping silently left all three unmapped. Added real `size`/`type` columns (populated at save and template-init time from the actual uploaded content/copied item, not guessed), an explicit `@Mapping` for `modifiedAt`, extracted content-type detection into a shared `util.ContentTypeUtils`, and backfilled every existing row from MinIO's real stored object metadata so already-created projects were fixed immediately rather than only on their next edit. Found and fixed along the way: `ContentTypeUtils` was checking `URLConnection.guessContentTypeFromName` before its own `.ts`/`.tsx` override, and the JDK's built-in MIME table maps `.ts` to an MPEG Transport Stream video type — every TypeScript file in the project was being mistyped as video until the check order was reversed. Separately, `GET .../files/content` with a missing `path` query parameter was falling through to the generic `Exception` → 500 handler (`MissingServletRequestParameterException` had no specific handler) instead of a clear 400 — added one, following the existing typed-exception pattern._
