# 8. Decision History

There is no separate ADR (Architecture Decision Record) directory in this project — significant design decisions and their reasoning are documented inline, next to the code they affect, in this file, `docs/schema/`, and `docs/api/`. If a past choice looks strange, the "why" is almost always written down close to the "what" rather than in a separate log.
