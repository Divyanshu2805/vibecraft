# How to read this

Each phase below lists: what moved, where it went, where it's called from now, and what (if anything) stayed behind as a soak-period fallback. "Not yet moved" sections point back at `legacy-monolith/` with its original package path unchanged.
