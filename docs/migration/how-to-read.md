# How to read this

Each phase below lists: what moved, where it went, where it's called from now, and what (if anything) stayed behind as a soak-period fallback. "Not yet moved" sections point back at `legacy-monolith/` with its original package path unchanged. **That module was removed on 2026-08-11** (see "Removing the monolith" under Phase 4): every mention of `legacy-monolith` below describes the original backend as it was, and it can be recovered from git history.
