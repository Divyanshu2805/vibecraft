package com.java.vibecraft.dto.idea;

/** The project brief compiled from an idea and its interview answers; sent as the project's first chat message. */
public record CompileIdeaResponse(
        String spec
) {
}
