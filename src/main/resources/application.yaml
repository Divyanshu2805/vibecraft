spring:
  application:
    name: vibecraft

  datasource:
    url: jdbc:postgresql://localhost:8000/vibecraftDB
    username: ${DB_USERNAME:User}
    password: ${DB_PASSWORD:Password}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    defer-datasource-initialization: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: false

jwt:
  secret-key: ${JWT_SECRET:as9b7aodlbjfkluosfd789ujeklfnlsduf90b8ha908w74tuqijvlckajdl}

stripe:
  api:
    secret: ${STRIPE_SECRET:sk_test_replace_me}
  webhook:
    secret: ${STRIPE_WEBHOOK_SECRET:whsec_replace_me}

client.url: http://localhost:8080